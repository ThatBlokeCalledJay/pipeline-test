parameters:
  - name: name
    type: string
  - name: solutionFile
    type: string

jobs:
- job:
  displayName: ${{ parameters.name }}
  steps:
  - script: echo '${{ parameters.solutionFile }} The yaml file was updated'
      